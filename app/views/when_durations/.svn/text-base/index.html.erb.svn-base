<h1>Listing when_durations</h1>

<table>
  <tr>
    <th>Subfctr name</th>
    <th>Description</th>
  </tr>

<% @when_durations.each do |when_duration| %>
  <tr>
    <td><%=h when_duration.subfctr_name %></td>
    <td><%=h when_duration.description %></td>
    <td><%= link_to 'Show', when_duration %></td>
    <td><%= link_to 'Edit', edit_when_duration_path(when_duration) %></td>
    <td><%= link_to 'Destroy', when_duration, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New when_duration', new_when_duration_path %>
